apiVersion: apps/v1
kind: Deployment
metadata:
  name: payroll-mcp
  namespace: department-assistant
  labels:
    app: payroll-mcp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payroll-mcp
  template:
    metadata:
      labels:
        app: payroll-mcp
    spec:
      containers:
        - name: payroll-mcp
          image: payroll-mcp:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 2
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: payroll-mcp
  namespace: department-assistant
spec:
  selector:
    app: payroll-mcp
  ports:
    - name: http
      port: 80
      targetPort: http
