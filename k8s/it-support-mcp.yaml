apiVersion: apps/v1
kind: Deployment
metadata:
  name: it-support-mcp
  namespace: department-assistant
  labels:
    app: it-support-mcp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: it-support-mcp
  template:
    metadata:
      labels:
        app: it-support-mcp
    spec:
      containers:
        - name: it-support-mcp
          image: it-support-mcp:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 2
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: it-support-mcp
  namespace: department-assistant
spec:
  selector:
    app: it-support-mcp
  ports:
    - name: http
      port: 80
      targetPort: http
