apiVersion: apps/v1
kind: Deployment
metadata:
  name: it-support-agent
  namespace: department-assistant
  labels:
    app: it-support-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: it-support-agent
  template:
    metadata:
      labels:
        app: it-support-agent
    spec:
      containers:
        - name: it-support-agent
          image: it-support-agent:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
          env:
            - name: LLM_BASE_URL
              value: http://agentgateway-proxy.agentgateway-system.svc.cluster.local/llm/gemini
            - name: MCP_SERVER_URL
              value: http://agentgateway-proxy.agentgateway-system.svc.cluster.local/mcp/it-support
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 2
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: it-support-agent
  namespace: department-assistant
spec:
  selector:
    app: it-support-agent
  ports:
    - name: http
      port: 80
      targetPort: http
