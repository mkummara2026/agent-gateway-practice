apiVersion: apps/v1
kind: Deployment
metadata:
  name: payroll-agent
  namespace: department-assistant
  labels:
    app: payroll-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payroll-agent
  template:
    metadata:
      labels:
        app: payroll-agent
    spec:
      containers:
        - name: payroll-agent
          image: payroll-agent:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
          env:
            - name: LLM_BASE_URL
              value: http://agentgateway-proxy.agentgateway-system.svc.cluster.local/llm/gemini
            - name: MCP_SERVER_URL
              value: http://agentgateway-proxy.agentgateway-system.svc.cluster.local/mcp/payroll
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 2
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: payroll-agent
  namespace: department-assistant
spec:
  selector:
    app: payroll-agent
  ports:
    - name: http
      port: 80
      targetPort: http
